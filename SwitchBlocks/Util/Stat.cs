namespace SwitchBlocks.Util
{
    /// <summary>Stats that can be checked for in the threshold type block.</summary>
    public enum Stat : byte
    {
        Jumps,
        Falls,
        Time,
        Session,
    }
}
